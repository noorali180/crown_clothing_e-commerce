import './sign-in.scss';

const SignIn = () => {
    
    return (
        <div>
            This is sign in page...
            <button onClick= {()=>{}} >Click to sign in with google.</button>
        </div>
    );
}

export default SignIn;
